name: Build Cross-Platform Executables

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build executable (Windows)
        if: matrix.os == 'windows-latest'
        run: |
          $sp = python -c "import site; print(site.getsitepackages()[-1])" | Out-String; $sp = $sp.Trim()
          Write-Host "Using site-packages: $sp"
          pyinstaller --onefile --clean `
            --hidden-import pygame `
            --add-data "$sp/pygame;pygame" `
            --add-data "data;data" `
            --add-data "assets;assets" `
            --name plaguefire `
            main.py
        shell: pwsh

      - name: Build executable (Linux/macOS)
        if: matrix.os != 'windows-latest'
        run: |
          SP=$(python -c "import site; p = site.getsitepackages()[0]; print(p)")
          echo "site-packages: $SP"
          pyinstaller --onefile --clean \
            --hidden-import pygame \
            --add-data "${SP}/pygame:pygame" \
            --add-data "data:data" \
            --add-data "assets:assets" \
            --name plaguefire \
            main.py
        shell: bash

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ runner.os }}-build
          path: dist/*

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    # This job only runs when a tag is pushed
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write # Required to create a release
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: builds

      - name: List downloaded files
        run: ls -R builds

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            builds/Linux-build/plaguefire
            builds/macOS-build/plaguefire
            builds/Windows-build/plaguefire.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
